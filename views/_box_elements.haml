- elements.each_with_index do |element, index|
  .grid_4{:class => "#{index % 2 ? 'alpha' : 'omega'}"}
    %blockquote.box
      %p
        %i{ :class => "#{element[:icon]}" }
          = shorten(element[:text], 275)

